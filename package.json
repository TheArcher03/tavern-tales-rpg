{
  "name": "tavern-tales-rpg",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,magenta \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server",
    "test": "npm run test -w shared"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
